export default class User {
  constructor(id, firstName, lastName, age, city) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.age = age;
    this.city = city;
  }
}
